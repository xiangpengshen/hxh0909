<view
  class=" custom-class iconfont icon-{{ name }} "
  style="{{ color ? 'color: ' + color : '' }}; {{ size ? 'font-size: ' + size : '' }}; {{ block ? 'display:block ' : '' }}"
  bind:tap="onClick"
>
  <view wx:if="{{ info !== null }}" class="icon__info">{{ info }}</view>
</view>
